# TRADEPO - Инвестиционная Платформа

## Описание проекта

TRADEPO - это современная инвестиционная платформа, предоставляющая возможность пользователям инвестировать средства в различные тарифные планы и получать ежедневную прибыль. Проект включает многоязычный интерфейс (английский, русский, казахский и другие языки), админ-панель для управления пользователями, депозитами и выводами средств, а также реферальную систему.

### Основные функции

- **Многоязычный интерфейс**: поддержка нескольких языков (EN, RU, KZ, UZ, TJ)
- **Расчет прибыли**: калькулятор для расчета потенциальной прибыли от инвестиций
- **Тарифные планы**: различные инвестиционные тарифы (Базовый, Премиум, Элит)
- **Личный кабинет пользователя**: управление депозитами, выводами и рефералами
- **Админ-панель**: управление пользователями, транзакциями и контентом сайта
- **Реферальная система**: возможность привлечения новых пользователей и получения бонусов

## Технологический стек

- **Frontend**: React, TypeScript, Tailwind CSS, Radix UI
- **Backend**: Node.js, Express
- **База данных**: Drizzle ORM, Neon PostgreSQL
- **Аутентификация**: JWT, Passport.js
- **Локализация**: i18next

## Установка и запуск

### Требования

- Node.js (версия 16+)
- npm или yarn

### Установка зависимостей

```bash
# Установка зависимостей проекта
npm install
```

### Запуск проекта в режиме разработки

```bash
# Запуск клиентской части (Frontend)
npm run dev
```

Или используйте существующие настроенные воркфлоу в Replit:

1. Нажмите кнопку "Run" для запуска Dev Server
2. Приложение будет доступно по URL вашего Replit проекта

### Сборка проекта для production

```bash
# Сборка проекта
npm run build

# Запуск в production режиме
npm run start
```

## База данных

Проект использует:
- **Локальная разработка**: SQLite (файл создается автоматически)
- **Production**: PostgreSQL через Neon Database
- **ORM**: Drizzle ORM для типобезопасной работы с базой данных

### Миграции базы данных
Схема базы данных находится в файле `shared/schema.ts`. При первом запуске таблицы создаются автоматически.

### Инициализация базы данных
При первом запуске приложения база данных автоматически инициализируется с тестовыми данными:
- Тарифные планы (Базовый, Премиум, Элит)
- Тестовый администратор (admin/admin)

## Структура проекта

```
├── client/               # Клиентская часть (React)
│   ├── src/
│   │   ├── components/   # UI компоненты
│   │   ├── i18n/         # Локализация
│   │   ├── pages/        # Страницы приложения
│   │   ├── hooks/        # React хуки
│   │   └── lib/          # Утилиты и вспомогательные функции
├── server/               # Серверная часть (Express)
│   ├── index.ts          # Точка входа сервера
│   └── routes.ts         # API маршруты
└── shared/               # Общие модули для клиента и сервера
```

## Локализация

Проект поддерживает несколько языков:

- Английский (EN)
- Русский (RU)
- Казахский (KZ)
- Узбекский (UZ)
- Таджикский (TJ)

Файлы локализации находятся в директории `client/src/i18n/locales/`.

## Возможные проблемы и решения

При запуске проекта могут возникнуть следующие ошибки:

1. **Ошибки в файлах локализации** - проверьте синтаксис в файлах локализации, особенно в `ru.ts` и `kz.ts`, где могут быть дубликаты ключей.
2. **Порт уже используется** - убедитесь, что порт 5000 не занят другими приложениями.

## Лицензия

MIT
# TRADEPO Investment Platform

## Описание проекта
TRADEPO - это платформа для инвестиций с высокой ежедневной доходностью до 15%. Поддерживает несколько языков: русский, английский, таджикский, казахский и узбекский.

## Особенности
- Многоязычный интерфейс (5 языков)
- Система тарифов с разной доходностью
- Реферальная программа
- Админ панель для управления
- Адаптивный дизайн
- Безопасность и защита данных

## Технологии
- **Frontend**: React 18, TypeScript, Tailwind CSS, Vite
- **Backend**: Node.js, Express, TypeScript
- **База данных**: SQLite с Drizzle ORM
- **Интернационализация**: react-i18next
- **UI Компоненты**: shadcn/ui
- **Иконки**: Lucide React

## Установка и запуск

### Локальная разработка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd tradepo-platform
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите проект в режиме разработки:
```bash
npm run dev
```

Сайт будет доступен по адресу: http://localhost:5000

### Развертывание на Render

1. Подключите ваш GitHub репозиторий к Render
2. Используйте следующие настройки:
   - **Build Command**: `npm run build`
   - **Start Command**: `npm start`
   - **Environment**: Node.js

### Переменные окружения

Создайте файл `.env` в корне проекта:
```
NODE_ENV=production
PORT=5000
DATABASE_URL=./database.sqlite
```

## База данных

Проект использует:
- **Локальная разработка**: SQLite (файл создается автоматически)
- **Production**: PostgreSQL через Neon Database
- **ORM**: Drizzle ORM для типобезопасной работы с базой данных

### Миграции базы данных
Схема базы данных находится в файле `shared/schema.ts`. При первом запуске таблицы создаются автоматически.

### Инициализация базы данных
При первом запуске приложения база данных автоматически инициализируется с тестовыми данными:
- Тарифные планы (Базовый, Премиум, Элит)
- Тестовый администратор (admin/admin)

## Структура проекта

```
├── client/                 # Frontend React приложение
│   ├── src/
│   │   ├── components/     # React компоненты
│   │   ├── pages/          # Страницы приложения
│   │   ├── i18n/           # Файлы переводов
│   │   ├── lib/            # Утилиты и хелперы
│   │   └── types/          # TypeScript типы
├── server/                 # Backend Express сервер
│   ├── index.ts           # Главный файл сервера
│   ├── routes.ts          # API маршруты
│   └── storage.ts         # Работа с базой данных
├── shared/                # Общие типы и схемы
└── package.json           # Зависимости проекта
```

## Языки интерфейса

Поддерживаемые языки:
- Русский (ru) - по умолчанию
- English (en)
- Тоҷикӣ (tj)
- Қазақша (kz)
- O'zbekcha (uz)

## API Endpoints

- `GET /api/tariffs` - Получить список тарифов
- `POST /api/deposits` - Создать депозит
- `GET /api/deposits/:userId` - Получить депозиты пользователя
- `POST /api/withdrawals` - Создать заявку на вывод
- `GET /api/users/:id` - Получить данные пользователя

## Админ панель

Доступна по адресу `/admin` для пользователей с правами администратора.

Функции:
- Управление пользователями
- Просмотр депозитов
- Обработка заявок на вывод
- Настройка тарифов
- Управление контентом

## Безопасность

- Валидация всех входных данных
- Защита от XSS атак
- Безопасное хранение паролей
- Ограничение скорости запросов

## Поддержка

Для получения поддержки обращайтесь:
- Telegram: @support
- Email: support@tradepo.ru

## Лицензия

© 2023 TRADEPO.RU. Все права защищены.